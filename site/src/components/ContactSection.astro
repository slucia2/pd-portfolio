<section id="contact" class="contact">
  <div class="contact-card">
    <div class="contact-copy">
      <h2>Let&rsquo;s build something people love to use.</h2>
      <p>Open to full-time and contract work across systems, enterprise, internal, and consumer tools. Email is the fastest way to reach me.</p>
    </div>
    <a class="btn-email" href="mailto:samuel.d.lucia@gmail.com">
      <span class="btn-email-ico" aria-hidden="true">✉</span> EMAIL SAM
    </a>
  </div>
</section>

<style>
  .contact { padding: 40px 72px 80px; scroll-margin-top: 80px; }
  .contact-card {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 32px;
    padding: 32px 40px;
    border: 1.5px solid rgba(29, 42, 51, .16);
    border-radius: 18px;
    background: #fff;
  }
  .contact-copy { max-width: 560px; }
  .contact-copy h2 {
    font-size: 24px;
    font-weight: 700;
    color: var(--ink, var(--v3-ink, #1c1b1f));
    letter-spacing: -.01em;
    margin: 0;
  }
  .contact-copy p {
    margin-top: 10px;
    font-size: 15px;
    line-height: 1.55;
    color: var(--muted, var(--v3-muted, #6e6c75));
  }
  .btn-email {
    flex: none;
    display: inline-flex;
    align-items: center;
    gap: 9px;
    padding: 14px 20px;
    background: var(--gold, #f5c84b);
    color: var(--ink, var(--v3-ink, #1c1b1f));
    border-radius: 10px;
    font-family: var(--font-pixel, var(--v3-mono, 'JetBrains Mono', ui-monospace, monospace));
    font-size: 10px;
    letter-spacing: .04em;
    text-decoration: none;
    box-shadow: 0 2px 0 var(--gold-dark, #b8952a);
    transition: transform .15s, box-shadow .15s;
  }
  .btn-email:hover {
    transform: translateY(-2px);
    box-shadow: 0 4px 0 var(--gold-dark, #b8952a);
    color: var(--ink, var(--v3-ink, #1c1b1f));
  }
  @media (max-width: 640px) {
    .contact { padding: 32px 24px 64px; }
    .contact-card { flex-direction: column; align-items: flex-start; gap: 22px; }
  }
</style>
